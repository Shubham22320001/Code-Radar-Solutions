#include <stdio.h>

int main() {
    int num1, num2, sum;

    // Taking input from the user
    scanf("%d", &num1);

    scanf("%d", &num2);

    // Calculating the sum
    sum = num1 + num2;

    // Printing the sum
    printf("Sum: %d\n",sum);

    return 0;
}